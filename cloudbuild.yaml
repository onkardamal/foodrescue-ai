steps:
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']
  
  - name: 'gcr.io/cloud-builders/npm'
    args: ['run', 'build']
  
  - name: 'gcr.io/firebase-tools:latest'
    args: ['deploy', '--only', 'hosting']
    env:
      - 'FIREBASE_TOKEN=${_FIREBASE_TOKEN}'
      - 'PROJECT_ID=${PROJECT_ID}'

substitutions:
  _FIREBASE_TOKEN: ''
options:
  machineType:  'E2_HIGHCPU_8'
    logging: CLOUD_LOGGING_ONLY
